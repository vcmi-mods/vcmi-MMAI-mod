{
  "$comment": [
    "Adjust temperature to alter model behaviour:",
    "  0.0 = greedy (deterministic)",
    "  1.0 = baseline",
    "  2.0 = uncertain",
    "  2.5 = dumb",
    "  3+ = garbage (too random)"
  ],
  "models": {},
  "fallback": "BattleAI",
  "temperature": 0.5,
  "seed": 0
}
